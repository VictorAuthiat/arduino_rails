
<%= simple_form_for(@measure) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :temperature %>
    <%= f.input :humidity %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
